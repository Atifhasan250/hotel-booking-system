# M4 checkpoint — production public experience and technical SEO

## Existence pre-flight (2026-08-28)

- **Verdict**: `PARTIAL`
- **Git state**: `package-lock.json` has a pre-existing install-generated diff; preserve it and do not attribute it to M4.
- **Existing evidence**:
  - `app/page.tsx` preserves the approved premium green/lime editorial homepage direction and interactive search controls.
  - `app/search/page.tsx` and `/api/v1/search` exist, but the page is client-only, its response types do not match the API, filters are placeholders, and its styling does not use the established design system.
  - Published catalog, rooms, approved ImageKit media, destinations, nearby places, rates, offers, and review aggregate collections exist or are represented by M2/M3 contracts.
  - Root metadata is minimal and no property/destination routes, sitemap, robots route, breadcrumb/structured-data helpers, public loading/error/not-found states, or M4 tests exist.
  - The homepage still contains hard-coded prices, ratings, reviews, unsupported `Apartment` inventory, and 14 unique `bookmyroom.site/wp-content` references plus placeholder-avatar hotlinks.
- **Controlled blockers**:
  - Launch districts, verified property/destination copy and image rights, launch language, and map provider remain owner decisions. M4 may implement truthful empty/test-fixture states and the ImageKit-backed contract, but must not invent production content or claim migration of assets that have not been rights-approved and uploaded.

## Scoped implementation plan

- **Outcome**: extend M2/M3 read models into server-rendered public discovery, destination and property surfaces; establish truthful SEO/structured data and safe responsive media; remove reference-host/placeholder hotlinks without fabricating production inventory, ratings, or prices.
- **Freeze boundary**: public app routes/components/styles, read-only catalog discovery contracts/adapters, metadata/SEO helpers, ImageKit presentation helpers, and M4 unit/integration/E2E evidence only.
- **Tests**: regression-first tests for published-only discovery, approved media, truthful review/offer schema, canonical/noindex behavior, public route states, keyboard/mobile behavior, and absence of prohibited hotlinks.
- **Risks**: accidental unpublished-data exposure, stale availability claims, fabricated structured data, client bundle leakage of Mongo configuration, visual regression, and unapproved media/content.
- **Rollback**: remove M4-only public discovery files/routes/tests and restore the M4-touched homepage/search composition; leave M0–M3 modules and the pre-existing lockfile diff intact.
- **Permitted verification**: targeted Vitest/Playwright during iteration, then `npm run typecheck`, `npm run lint`, `npm run test`, `npm run test:integration`, and `npm run test:e2e`. Never run `npm run build`.

## Iteration 1 implementation and maker evidence (2026-08-28)

- Added a published-only public catalog application port/service and MongoDB adapter for property cards, destination
  pages, property details, active rooms, verified nearby places, approved ImageKit media, review-safe empty states,
  integer-minor-unit starting rates, and sitemap entries.
- Added server-rendered property/destination routes, conservative server search, unique canonical/OG metadata,
  truthful conditional JSON-LD, production/staging robots policy, database-backed sitemap, breadcrumbs, and public
  loading/error/not-found states.
- Evolved the homepage without changing its green/lime/editorial/rounded design system: live published discovery is
  loaded through a thin public API, the search form submits dates/guests/rooms, Next Image has explicit responsive
  sizing, and all hard-coded properties/prices/ratings/testimonials plus reference-host/avatar hotlinks were removed.
- When launch content or the local database is absent, surfaces render explicit empty/unavailable states and never
  claim availability, a final quote, a review, payment success, or a bookable future service.
- Added unit, real-Mongo integration, SEO, homepage smoke, and desktop/tablet/mobile Playwright coverage.

### Final maker commands

- Environment: Windows workspace, Node project dependencies installed; Playwright Chromium installed after the first
  E2E attempt proved the executable was missing.
- `npm.cmd run typecheck` — exit 0.
- `npm.cmd run lint` — exit 0, zero warnings.
- `npm.cmd run test` — exit 0, 23 files / 77 tests.
- `npm.cmd run test:integration` — exit 0, 9 files / 45 tests, including real MongoDB public discovery.
- `npm.cmd run test:e2e` — exit 0, 17/17 tests, including homepage desktop/tablet/mobile, hotlink/fake-content,
  search submission, noindex, invalid-date, and existing auth/vendor/admin regression scenarios.
- `git diff --check` — exit 0; only line-ending notices were emitted by Git on the Windows worktree.
- `npm run build` was not run.

### Independent re-verification and owner authorization

- The first independent review rejected completion and found one P1 correctness defect: the public catalog adapter
  expected duplicate `id` fields even though canonical M2 persistence stores identity in MongoDB `_id`.
- The adapter and its real-Mongo fixture were corrected to use `_id` for identity and joins, then map `_id` to domain
  `id` at the boundary. Non-production page-level noindex, dynamic sitemap freshness, M7-gated review suppression,
  and conservative homepage copy were also re-checked.
- Fresh verifier result: correction set `APPROVE`; typecheck, lint, 9/9 focused unit/SEO/smoke tests and 12/12 focused
  public-discovery/search integration tests passed. The verifier edited no files and no build ran.
- The owner explicitly approved on 2026-08-28. Overall M4 nevertheless remains `PARTIAL` / not approved for
  completion because the independent verifier did not approve milestone completion: server-rendered homepage
  discovery, the full required homepage/detail/calendar/gallery/contact/filter surface, representative
  accessibility/performance evidence, and controlled content/provider inputs remain incomplete.

### Unresolved gates

- M4 remains `PARTIAL`: verified launch districts/copy/image rights, real approved ImageKit uploads, launch language,
  and the map/contact decisions have not been supplied. The local SVG is an explicit non-production media fallback,
  not claimed ImageKit migration evidence.
- Code inspection invalidated the prior M3 concurrency claim: hold creation reads inventory/holds and then inserts a
  separate hold without a contended inventory write or conditional per-night reservation. Sequential tests do not
  prove concurrent no-oversell. See `.genesis/deviations/2026-08-28-m3-atomic-hold-proof-gap.md`.
