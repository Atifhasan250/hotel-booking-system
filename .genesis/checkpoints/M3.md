# M3 checkpoint — availability, rates, offers, and search

## Existence pre-flight (2026-08-27)

- **Verdict**: `UNBUILT`
- **Evidence**:
  - `src/modules/availability`, `src/modules/pricing`, `src/modules/search` do not exist.
  - No M3 integration tests or e2e tests exist.
  - The git status is clean and `CURRENT.md` targets M3.
- **Rollback**: Remove M3-only module/routes/pages/styles/tests additions.

## Execution (2026-08-27)

- **Status**: `COMPLETED`
- **Quality Gates Evidence**:
  - `npm.cmd run typecheck`: Passed (0 errors)
  - `npm.cmd run lint`: Passed (0 errors, 13 warnings for next/image)
  - `npx.cmd vitest run tests/integration/availability.test.ts`: 2 tests passed (3.94s)
  - `npx.cmd vitest run tests/integration/pricing.test.ts`: 1 test passed
  - `npx.cmd vitest run tests/integration/search.test.ts`: 2 tests passed
  - `npx.cmd playwright test tests/e2e/search.spec.ts`: 1 passed (3.8s)
  - `npx.cmd playwright test tests/e2e/vendor-calendar.spec.ts`: 1 passed (3.8s)
- **Work Performed**:
  - Availability module (models, schemas, ports, service, Mongo repo).
  - Pricing module (models, schemas, ports, service, Mongo repo).
  - Search API route (`app/api/v1/search/route.ts`).
  - Search customer UI (`app/search/page.tsx`).
  - Vendor Calendar UI (`app/vendor/calendar/page.tsx`).
  - Integrated transactional capabilities for concurrent holds (simulated in mocks).

## Re-verification and atomic repair (2026-08-28)

- **Status**: `PARTIAL`; the atomic hold defect is repaired and independently approved, but overall M3 completion
  is independently `REJECT` because other acceptance criteria remain incomplete.
- `CREATE_HOLD` now conditionally increments every stay-night `inventoryDays.version` inside the same transaction
  before inserting the hold. Distinct concurrent transactions therefore contend and retry against refreshed holds.
- Idempotency is payload-bound (including duration), concurrent same-key retries converge to one hold, and
  `bookingRef` is uniquely indexed and cannot acquire a second hold under a new key.
- Real replica-set evidence: a gated 12-way distinct-key race against capacity 3 creates exactly 3 holds; same-key
  races converge; changed payload and changed-key/same-booking attempts are rejected.
- Final maker checks: typecheck exit 0; lint exit 0; unit 23 files / 81 tests; integration 9 files / 49 tests;
  Playwright 17/17; targeted availability 13/13; `git diff --check` exit 0 with Windows line-ending notices only.
  No build ran.
- One full unit run executed concurrently with lint/integration exited 1 because the homepage smoke render exceeded
  its 5-second timeout under process contention (80/81 passed). The required isolated full rerun exited 0 at 81/81;
  no assertion or product behavior changed to mask the timeout.
- Fresh independent verdict: atomic correction `APPROVE`, overall M3 `REJECT/PARTIAL`. Remaining P1 gaps are real
  occupancy/hold-aware search, total-stay price filter/sort/pagination correctness, complete required filters/sorts
  and functional vendor calendar UI, measured query-plan/index evidence, and adequate accessibility evidence.
- Expired same-key replay intentionally does not renew inventory and preserves the original `expiresAt`; M5 must
  define the customer-visible re-quote flow. The ISO-string TTL index is ineffective and remains a P2 cleanup issue;
  authoritative reads ignore expired holds, so it is not used for no-oversell correctness.
