# M1 checkpoint — identity, sessions, and tenant authorization

## Existence pre-flight — 2026-08-27 Asia/Dhaka

- Verdict: `UNBUILT`.
- Worktree: `main...origin/main`, clean before M1 edits (`git status --short --branch`, exit 0).
- Search evidence: no identity, session, password, permission, vendor membership, admin binding, audit, CSRF, or
  rate-limit implementation exists under `app/`, `src/`, or `tests/`. The only MongoDB references are the M0
  environment schema and architecture notes.
- Existing baseline preserved: `app/page.tsx`, `app/layout.tsx`, and `app/globals.css` remain the homepage visual
  reference. No nested `AGENTS.md` exists.
- Installed framework contract reviewed before planning route/auth work: Next.js 16.3.3 local guides for App Router
  authentication, async `cookies()`, Route Handlers, and forms.

## M1 implementation plan

- Outcome: runtime-validated registration/login/contact verification/recovery, opaque database sessions with
  rotation and revocation, deny-by-default customer/vendor/admin authorization, append-only audit events, abuse
  controls, and accessible auth routes matching the existing visual language.
- Boundary: only `identity`, `audit`, shared request security, MongoDB identity infrastructure, auth routes/UI,
  environment/setup documentation, and M1 tests. No catalog, availability, booking, payment, or dashboard work.
- Tests: domain/unit tests for normalization, password policy, token/session lifecycle, permissions, tenant/customer
  isolation, rate limits, and audits; integration-style use-case tests across the identity/audit ports; auth UI smoke
  coverage. Establish the M1 `test:integration` command without claiming an external MongoDB service was exercised.
- Primary risks: account enumeration, raw session/recovery-token disclosure, stale authorization claims, cross-tenant
  access, replay, open redirects, CSRF/login CSRF, weak password storage, and personal data in audit metadata.
- Rollback: remove M1-only routes/modules/scripts and revert additive environment/docs changes; M0 homepage files
  remain structurally untouched except for narrowly scoped shared auth styling if required.
- Verification commands: targeted Vitest while iterating, then `npm run typecheck`, `npm run lint`, `npm run test`,
  and `npm run test:integration`. `npm run build` is prohibited. `test:e2e` remains a reported gap unless M1 safely
  establishes it.

Milestone status is not complete. Independent verification and explicit owner authorization are still required.

## Implementation candidate — iteration 1

- Added runtime-validated registration, sign-in, contact verification, recovery/reset, logout, and session refresh
  use cases/routes. Passwords use salted Argon2id; browser cookies contain only opaque random session tokens whose
  SHA-256 digests are stored. Reset revokes every active session and tokens are one-time/expiry-bound.
- Added deny-by-default customer/vendor/admin/super-admin authorization, server-side current-grant resolution,
  vendor/customer isolation tests, and append-only audit writes for success, denial, replay, invalid token, and
  rate-limit outcomes.
- Added official MongoDB-driver repositories, transaction-based session rotation/token replacement, conditional
  atomic rate-limit buckets, and required unique/TTL/query indexes. `mongod` is not installed locally, so no real
  MongoDB/replica-set integration result is claimed.
- Added a provider-neutral encrypted identity-delivery queue with status `PENDING_PROVIDER_CONFIGURATION`. This is a
  bounded M1 bridge for verification/recovery; M10 must select and connect the approved notification provider. No
  token is logged or returned to the browser.
- Added a noindex, responsive auth page that extends the existing forest/lime/editorial visual direction. The only
  homepage change converts the existing Dashboard control into a link to `/auth`; existing layout/content remains.
- Unrelated concurrent user work observed after pre-flight was committed by the owner as HEAD `1bc2f0b` (navigation/
  footer logo styling, homepage `assets.logo`, tracked `public/bookmyroom-dark-no-bg.png`, and matching homepage smoke
  assertions). Three additional `public/bookmyroom*.png` files remain untracked and user-owned. None is M1 evidence.

### Commands and actual results

- `npm.cmd install mongodb @node-rs/argon2`: initial sandbox run timed out; approved network retry exited 0. The
  native Argon2 binding then failed its targeted test, so it was removed rather than bypassed.
- `npm.cmd install hash-wasm` / `npm.cmd uninstall @node-rs/argon2`: exit 0; portable Argon2id replacement installed.
- First targeted test: exit 1, correctly exposing pre-trim email validation and unusable native binding; both causes
  were corrected. Re-run: 4 files / 9 tests, exit 0.
- Final `npm.cmd run typecheck`: exit 0.
- Final `npm.cmd run lint`: exit 0 with 13 known M0 raw-image warnings and no errors.
- Final `npm.cmd run test`: exit 0, 11 files / 24 tests.
- Final `npm.cmd run test:integration`: exit 0, 2 files / 4 tests.
- `npm.cmd run test -- identity`: exit 0, 1 file / 3 tests.
- `npm.cmd run test:integration -- authz`: exit 0, 2 files / 4 tests.
- `Get-Command mongod ...`: exit 1/no output; local real-Mongo integration unavailable.
- Local `next dev` `/auth` readiness probe: HTTP 200. The governed in-app browser could not initialize its Node
  runtime (`os error 3`), so no desktop/tablet/mobile visual or interactive browser evidence is claimed.
- `npm run build`: not run, per governance.
- `npm run test:e2e`: not established; no pass is claimed.

### Impact and open evidence

- Security/privacy: no raw passwords/session tokens are persisted; queued verification/reset secrets use AES-256-GCM
  with an environment-injected key; audit metadata excludes email/IP/user-agent values; origin checks and rate limits
  protect every exposed mutation. Production proxy trust and secret rotation remain launch-hardening work.
- Accessibility/SEO: labeled inputs, live status, keyboard focus, 44px controls, responsive layout, and auth-page
  `noindex` are implemented and smoke-tested. No manual accessibility audit or browser E2E claim is made.
- Rollback: M1 files and additive scripts/env fields can be removed; no migration or production data was executed.
- Deviation note: the temporary `identityMessageDeliveries` collection is an identity-scoped encrypted handoff, not
  a selected notification provider. M10 must absorb/migrate it into the canonical durable notification workflow.
- Candidate remains `PARTIAL` pending independent verifier verdict and owner authorization.

## Independent verification — iteration 1

- Verdict: `REJECT`.
- Findings: audit was not transactionally coupled to critical mutations; forwarding headers were spoofable as rate
  keys; unknown-account login skipped Argon2 work; Mongo transactions lacked real evidence/capability probing; browser
  E2E was not established.

## Correction candidate — iteration 2

- Added an application transaction boundary backed by MongoDB `withTransaction`, `snapshot` reads, and majority
  writes. Registration, login/session creation, rotation, logout, verification, password reset/revocation, encrypted
  delivery enqueue, rate-limit denial, and their audit entries now commit or roll back together.
- Added startup composition probing that fails identity service/actor resolver initialization unless the configured
  MongoDB deployment supports transactions.
- Removed `x-real-ip` and `x-forwarded-for` from security decisions. Abuse control now uses non-spoofable global
  Mongo buckets plus hashed normalized account/token subjects; untrusted address headers are neither trusted nor
  recorded. Regression test proves header variation cannot change the security context.
- Unknown-account login now performs the same configured Argon2id hashing work before returning the generic invalid
  credential response. Regression coverage verifies the fake/test hasher is invoked for absent identities.
- Added rollback regression coverage that simulates audit failure and proves password, reset token, sessions, and
  audit state all roll back, followed by a successful retry.
- Added `mongodb-memory-server` replica-set integration coverage. Initial run failed honestly because the Windows VC++
  runtime was absent. With explicit approval, `winget install --id Microsoft.VCRedist.2015+.x64 ...` installed
  14.51.36247.0 (exit 0). Re-run `npm.cmd run test:integration -- mongo-transactions` exited 0: 3 files / 9 tests,
  including real rollback/commit, token consume-once, rotation, replay rejection, indexes, and capability probe.
- Browser E2E remains unestablished. The governed in-app browser runtime itself cannot start (`os error 3`); no
  Playwright substitute or visual/accessibility claim is made.

Status remains `PARTIAL` until fresh computed checks and independent re-verification. No milestone completion is
self-approved.

### Correction candidate final checks

- `npm.cmd run typecheck`: exit 0.
- `npm.cmd run lint`: exit 0 with the same 13 M0 raw-image warnings, no errors.
- `npm.cmd run test`: exit 0, 13 files / 30 tests, including the real Mongo replica set.
- `npm.cmd run test:integration`: exit 0, 3 files / 9 tests.
- `npm.cmd run test -- identity`: exit 0, 1 file / 5 tests.
- `npm.cmd run test:integration -- authz`: exit 0, 3 files / 9 tests.
- `git diff --check`: exit 0; line-ending notices only.
- `npm run build`: not run. `npm run test:e2e`: still not established and not claimed.

## Independent re-verification — iteration 2

- Verdict: `REJECT`.
- Remaining findings: missing browser/viewport evidence; audit records lacked actor identity and logout target; recovery
  request timing differed by account existence; real-Mongo tests did not prove standalone rejection or exact indexes.

## Correction candidate — iteration 3

- `AuditEvent.actorId` is mandatory. Identity events now record the authenticated/token-proven user when known and a
  stable `anonymous` actor otherwise; logout resolves and records its session user as both actor and target.
- Recovery requests no longer look up account existence synchronously. Every valid request takes the same path:
  global+hashed-subject throttling, encrypted provider-neutral queue insertion, and anonymous audit in one transaction.
  The queued normalized address is encrypted and only its SHA-256 recipient hash is separately stored.
- Regression coverage proves the recovery request path performs no user lookup for either existing or absent emails,
  emits the same queue operation, and retains generic HTTP semantics. Reset-token consumption remains separately
  covered with one-time, expiry, password, session-revocation, audit rollback, and retry tests.
- The real-Mongo suite now starts both a replica set and an unsupported standalone deployment. It proves the runtime
  capability probe accepts the replica set and rejects standalone Mongo, and asserts exact unique/TTL/tenant/audit
  index definitions in addition to transaction rollback/commit and replay behavior.
- Final checks after these corrections: `typecheck` exit 0; `lint` exit 0 with 13 known warnings; `test` exit 0,
  13 files / 32 tests; `test:integration` exit 0, 3 files / 11 tests; targeted identity and Mongo checks exit 0.
- Governed browser initialization was retried after installing the VC++ runtime and still fails before connection with
  `os error 3`. Browser E2E/desktop/tablet/mobile evidence is the sole known unclosed verification surface; it is not
  claimed or replaced with an unauthorized browser tool.

Status remains `PARTIAL` pending final independent verdict and explicit owner authorization.

## Final independent verification — iteration 3

- Verdict: `REJECT`.
- Sole P1 blocker: M1 touches auth UI but has no governed desktop/tablet/mobile regression evidence and no established
  `test:e2e` command. ADR-0005 and the all-conditions milestone gate do not permit waiving this evidence because the
  browser tool is unavailable.
- Verifier conclusion: no remaining P0/P1 server-side defect was identified in identity, authorization, audit,
  rate-limit, recovery, transaction capability, index, or real-Mongo evidence.
- Exact external tooling failure: the required in-app browser Node runtime fails before connection with `The system
  cannot find the path specified (os error 3)`; retry after VC++ runtime installation produced the same result.
- Stop state: M1 remains `PARTIAL`, not complete. Resume only by restoring the governed browser runtime, establishing
  real browser `test:e2e` and desktop/tablet/mobile evidence, rerunning computed checks, and obtaining a new verifier
  verdict. Explicit owner authorization would still be required to mark M1 done.
