# M5 — Quote, booking, inventory hold, and invoice core

## 2026-08-28 existence pre-flight

- Verdict: `UNBUILT`.
- Worktree: pre-existing user change in `package-lock.json`; later re-check also found concurrent changes in
  `README.md`, `package.json`, `tsconfig.json`, `scripts/`, and `tests/unit/bookmyroom-import.test.ts`. Preserve all
  of them and do not include them in M5 work.
- Existing reusable capability: integer BDT money/date primitives; immutable `PriceQuote` shape and Mongo repository;
  atomic, expiring, idempotent inventory holds with real replica-set concurrency evidence; customer identity/RBAC.
- Missing: booking module, lifecycle/state-event model, booking boundary validation, customer-scoped repository, policy
  snapshot, invoice model/rendering, booking route/UI, and M5-specific unit/integration/E2E evidence.
- Controlled blockers: cancellation/refund and property/platform policy hierarchy; tax/fee rules; legal merchant and
  invoice identity/numbering. These must not be defaulted or invented. Safe contracts may require explicit snapshots
  while production quote/invoice issuance remains disabled until owner-approved values exist.
- Scope for first iteration: framework-independent domain types, strict input schemas, transition guard, immutable
  snapshot construction, and regression tests. No payment/EPS behavior and no future dashboard scope.
- Risks: existing M3/M4 partial acceptance limits end-to-end booking claims; inventory hold orchestration must remain
  transaction-safe; snapshots must never retain mutable input references or floating-point money.
- Rollback: remove the new `src/modules/booking` files and their focused tests/checkpoint entry; no stored data or
  migration is introduced in this iteration.
- Planned checks: focused Vitest, `npm run typecheck`, `npm run lint`, `npm run test`, and established integration
  tests. Per governance, never run `npm run build`.

## 2026-08-28 iteration 1 — domain foundation

- Added `src/modules/booking/domain`: canonical lifecycle types, append-only state-event contract, strict booking
  draft/guest/consent validation, an explicit transition guard, and immutable quote/policy snapshot constructors.
- Quote arithmetic is derived exclusively from non-negative safe integer minor units. Nightly inconsistencies,
  fractional values, missing consent, invalid local-date order, and lifecycle skips/reversals are rejected.
- Snapshot constructors copy and recursively freeze caller input, preventing later rate/policy object mutation from
  rewriting booking-time facts. Tax/fee lines require explicit rule revisions; no rate or legal identity was invented.
- Context invariants checked: domain has no Next.js/provider/Mongo imports; money remains integer BDT; payment success
  is not implemented or claimed; no cross-tenant route/data surface was introduced; existing design is untouched.
- Commands (Windows `npm.ps1` was blocked by execution policy, so `npm.cmd` was used):
  - `npm.cmd run test -- booking-core`: exit 0, 1 file / 9 tests.
  - `npm.cmd run typecheck`: exit 0.
  - `npm.cmd run lint`: exit 0.
  - `npm.cmd run test`: exit 0, 25 files / 92 tests.
  - `npm.cmd run test:integration`: exit 0, 9 files / 49 tests.
  - `npm run build`: intentionally not run.
- State: `PARTIAL`. No independent verifier approval has been requested, and milestone completion is not claimed.
- Next: introduce authorized booking application/repository contracts and an atomic booking+hold integration test;
  quote issuance, invoice identity/numbering, cancellation evaluation, UI and E2E remain decision/evidence gated.

## 2026-08-28 iteration 2 — researched policy/tax/invoice contracts

- Direct owner instruction authorized researching comparable practice and adopting one approach. ADR-0006 records
  sources, rationale, effective scope, rollback, and the remaining merchant-specific verification gate.
- Added `FLEXIBLE_48H_V1`: property-approved policy overrides the fallback; otherwise cancellation at/before the
  48-hour cutoff has no room penalty, while later cancellation/no-show charges one discounted room night. Explicit
  non-refundable fees remain chargeable; all amounts are capped at the immutable quote total.
- Added effective-dated, admin-approved merchant tax profiles with ordered integer-basis-point rules and explicit
  tax basis/refundability. Draft, unapproved, expired/future, unsupported, or fractional configuration fails closed.
  No statutory rate or convenience fee is silently applied.
- Added verified merchant invoice identity gate, BIN requirement for VAT-registered merchants, immutable invoice
  snapshot, quantity-safe nightly lines, and `BMR-{MERCHANT_CODE}-{YYYY}-{8_DIGIT_SEQUENCE}` format. Persistence
  must allocate that sequence atomically in the next application/repository iteration.
- Verification: `npm.cmd run test -- booking-core booking-policy-invoice` exit 0 (2 files/15 tests); typecheck and
  lint exit 0; `npm.cmd run test` exit 0 (26 files/100 tests); `npm.cmd run test:integration` exit 0 (9 files/49
  tests). `npm run build` was intentionally not run.
- State remains `PARTIAL`: contracts are tested, but booking persistence/hold orchestration, invoice persistence/
  rendering/download, customer route/UI/E2E and independent verification are not complete.
