# M0 checkpoint — Adopt the existing frontend safely

- preflight_date: 2026-08-27 Asia/Dhaka
- preflight_verdict: PARTIAL
- milestone_state: COMPLETE; independent verification APPROVED
- freeze_boundary: governance/docs, package tooling, test/config scaffolding, shared platform skeleton; no redesign

## Existence pre-flight evidence

- Existing: Next.js 16.3.3 App Router homepage in `app/page.tsx`, global visual system in `app/globals.css`, strict
  TypeScript, React 19 interactions, mobile breakpoints, reduced-motion CSS.
- Missing before M0: lint configuration/script, test runner/script, tests, validated environment schema, platform/
  module boundary skeleton, remote asset inventory, milestone checkpoint, and complete non-build setup guidance.
- Backend truth absent: MongoDB repositories, ImageKit ownership, authentication, catalog, availability, booking,
  EPS payments, dashboards, CMS, and durable jobs. These remain in later milestones.
- Worktree before edits: `.genesis/`, `AGENTS-GENESIS-SNIPPET.md`, and `GENESIS-README.md` were untracked and treated
  as user-provided work to preserve. Branch `main`; HEAD `a4de1d0`.
- Baseline command: `npm.cmd run typecheck` exited 0. `lint` and `test` scripts did not exist.
- Asset scan: 19 remote image occurrences / 17 unique image URLs; see `inventories/remote-assets.md`.

## M0 implementation plan

1. Add installed-version Next.js ESLint flat configuration and Vitest/Testing Library scaffolding.
2. Add homepage structure/interaction smoke coverage and foundational money/env contract unit tests.
3. Add validated server-only environment access, module/platform boundaries, and secret-free `.env.example`.
4. Encode the existing design as a binding ADR-0005 preservation contract.
5. Mark Tour/Car controls non-bookable without redesigning the homepage.
6. Run `npm run typecheck`, `npm run lint`, and `npm run test`; never run a build.
7. Obtain a separate verifier verdict, then update milestone evidence and current state.

## Files in scope

- Tooling/config: `package.json`, `package-lock.json`, `eslint.config.mjs`, `vitest.config.mts`, `.env.example`
- Tests: `tests/setup.ts`, `tests/smoke/homepage.test.tsx`, `tests/unit/*`
- Foundation: `src/platform/**`, `src/shared/money/**`, `src/modules/README.md`
- Safety-only homepage delta: `app/page.tsx`, `app/globals.css`
- Governance/evidence: ADR-0005, this checkpoint, asset inventory, README, implementation notes, traceability, CURRENT

## Risks and rollback

- Test DOM differences may expose browser-only assumptions; fix from evidence without weakening the smoke contract.
- Existing plain `<img>` and remote assets may produce lint/performance debt; inventory now, migrate in M4 without
  replacing the visual direction.
- Env parsing must never enter client bundles or print secret values.
- Rollback is removal of M0 scaffolding and the small Coming-soon safety delta; no data migration is involved.

## Implementation evidence — 2026-08-27

- Added Next.js 16 ESLint flat config and Vitest/Testing Library scaffolding using locally installed framework docs.
- Added server-only validated environment access, secret-free environment template, module/platform boundaries, and
  an integer-minor-unit BDT money primitive.
- Added 10 tests across homepage smoke, environment validation, and money invariants.
- Encoded the existing frontend foundation in ADR-0005 and inventoried all remote image references.
- Preserved homepage layout/styles; limited UI changes to semantic tab roles and explicit future-service safety.

Commands on the revised candidate:

- `npm.cmd run typecheck` — exit 0.
- `npm.cmd run lint` — exit 0; 13 `@next/next/no-img-element` warnings remain as the documented M4 ImageKit/
  responsive-image migration debt.
- `npm.cmd run test` — exit 0; 3 files, 10 tests.
- `git diff --check` — no whitespace errors; PowerShell reported expected LF-to-CRLF worktree notices.
- `npm.cmd ls --depth=0` — exit 0; declared dependency tree resolved.
- Secret-pattern scan — only explicit placeholder/test key names and values; no production credential evidence.

## Independent verification history

- Review 1: `REJECT`. The verifier found a priced Tour sample/filter, external `/tours/` link, misleading Car copy,
  missing Tour regression assertion, and stale closeout records.
- Maker correction: converted the sample/filter to in-scope Eco Resort, removed external Tour navigation and live-Car
  implication, labeled future surfaces clearly, covered both Tour and Car, and synchronized closeout records.
- Review 2: `APPROVE`. The fresh verifier reran typecheck, lint, tests, dependency/diff checks, asset counts and
  secret hygiene; confirmed the corrected Tour/Car boundary and homepage preservation; and ran no build command.

## Final verifier verdict

`APPROVE` — M0 outcome and acceptance criteria are demonstrated. Non-blocking debt is explicitly deferred to its
owned milestones: 13 raw-image/ImageKit warnings and hard-coded content to M4; production backend, integration, and
E2E environments to later milestones. No M1 implementation is included in this checkpoint.
